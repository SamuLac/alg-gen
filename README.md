# [PT-BR]
# Algoritmo Genético – Otimizador F6

## 🧩 Descrição   
Este projeto implementa um Algoritmo Genético (AG) projetado para otimizar a função de benchmark F6 utilizando codificação binária, seleção por roleta, elitismo, crossover de um ponto e mutação por inversão de bits(flip-bit).

---

## ⚙️ Requisitos  
Certifique-se de ter o **Python 3.8 ou superior** instalado em seu sistema.  
Você pode verificar sua versão com:
```bash
python --version
```
ou  
```bash
python3 --version
```

---

## 📦 Instalação e Configuração  

### 🪟 Windows
1. **Crie o ambiente virtual:**
   ```bash
   python -m venv venv
   ```
2. **Ative o ambiente virtual:**
   ```bash
   venv\Scripts\activate
   ```
3. **Instale as dependências:**
   ```bash
   pip install -r requirements.txt
   ```
4. **Execute o programa:**
   ```bash
   python ga_f6_optimizer.py
   ```

---

### 🍎 macOS / 🐧 Linux
1. **Crie o ambiente virtual:**
   ```bash
   python3 -m venv venv
   ```
2. **Ative o ambiente virtual:**
   ```bash
   source venv/bin/activate
   ```
3. **Instale as dependências:**
   ```bash
   pip install -r requirements.txt
   ```
4. **Execute o programa:**
   ```bash
   python3 ga_f6_optimizer.py
   ```

---

## 🚀 Observações
- Para desativar o ambiente virtual, use:
  ```bash
  deactivate
  ```
- Você pode modificar parâmetros (tamanho da população, taxa de mutação, número de gerações etc.) diretamente no arquivo `ga_f6_optimizer.py`.


---

# [EN-US]
# Genetic Algorithm – F6 Optimizer

## 🧩 Description   
This project implements a Genetic Algorithm (GA) designed to optimize the F6 benchmark function using binary encoding, roulette selection, elitism, one-point crossover, and bit-flip mutation.

---

## ⚙️ Requirements  
Make sure you have **Python 3.8 or higher** installed on your system.  
You can check your version with:
```bash
python --version
```
or  
```bash
python3 --version
```

---

## 📦 Installation and Setup  

### 🪟 Windows
1. **Create the virtual environment:**
   ```bash
   python -m venv venv
   ```
2. **Activate the virtual environment:**
   ```bash
   venv\Scripts\activate
   ```
3. **Install dependencies:**
   ```bash
   pip install -r requirements.txt
   ```
4. **Run the program:**
   ```bash
   python ga_f6_optimizer.py
   ```

---

### 🍎 macOS / 🐧 Linux
1. **Create the virtual environment:**
   ```bash
   python3 -m venv venv
   ```
2. **Activate the virtual environment:**
   ```bash
   source venv/bin/activate
   ```
3. **Install dependencies:**
   ```bash
   pip install -r requirements.txt
   ```
4. **Run the program:**
   ```bash
   python3 ga_f6_optimizer.py
   ```

---

## 🚀 Notes
- To deactivate the virtual environment, use:
  ```bash
  deactivate
  ```
- You can modify parameters (population size, mutation rate, generations, etc.) directly in the `ga_f6_optimizer.py` file.
